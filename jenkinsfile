pipeline {
    agent any
    
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("jenkins-demo/ml_model:4", "/home/ubuntu .")
                }
            }
        }
    }
}
